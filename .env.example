# Uplift AI public blog API token (scoped to the selected website).
# Copy this file to `.env.local` and paste your real token below.
# Kept server-side only — never exposed to the browser.
UPLIFT_BLOG_TOKEN=your-token-here
